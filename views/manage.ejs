<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Management</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/manage.css" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/admin">User Management</a>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/make">서버 컨테이너 현황</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manage">전체 사용자 관리</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/board">게시판 관리</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/">로그아웃</a>
          </li>
        </ul>
      </div>
    </nav>

    <div class="container mt-5">
      <h1>User Management</h1>
      <ul class="list-group mt-3">
        <% users.forEach(user => { %>
        <li
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <%= user.name %> (Level: <%= user.level %>) <% if (user.level !== 3) {
          %>
          <form action="/updateLevel/<%= user._id %>" method="post">
            <select class="form-control form-control-sm mr-2" name="level">
              <option value="1">Level 1</option>
              <option value="2">Level 2</option>
            </select>
            <button type="submit" class="btn btn-primary btn-sm mr-2">
              Update Level
            </button>
          </form>
          <% } %>
          <form action="/removeUser/<%= user._id %>" method="post">
            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
          </form>
        </li>
        <% }); %>
      </ul>
    </div>
  </body>
</html>
